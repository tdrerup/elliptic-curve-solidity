import type { ResolverSource } from "../../source";
export declare class Truffle implements ResolverSource {
    options: any;
    constructor(options: any);
    resolve(importPath: string): Promise<{
        body: string;
        filePath: string;
    }>;
    require(): null;
    resolveDependencyPath(_importPath: string, dependencyPath: string): string;
}
