import type { ContractObject } from "@truffle/contract-schema/spec";
import type { ResolverSource } from "../source";
export declare class EthPMv1 implements ResolverSource {
    workingDirectory: string;
    constructor(workingDirectory: string);
    require(importPath: string): ContractObject;
    resolve(importPath: string): Promise<{
        body: string;
        filePath: string;
    }>;
    resolveDependencyPath(importPath: string, dependencyPath: string): string;
}
